import { ThemeFileSystemEntry } from "common/types";

export class FileSystemManager {
  private entries: Map<string, ThemeFileSystemEntry>;
  constructor() {
    this.entries = new Map<string, ThemeFileSystemEntry>();
  }




}
